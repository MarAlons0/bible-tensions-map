function initTimeline() {
  // Timeline items are server-rendered; this just adds any client-side enhancements.
  // For now: nothing extra needed — the server renders them in canonical order with dating labels.
  // Future: re-sort by scholarly dating parsed from dating string.
}
